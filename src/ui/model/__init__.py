"""Model layer for UI-related configuration.

This package hosts configuration data (YAML) and UI schemas used by
the Qt views.  It is part of the MVC refactor where models live under
``src/ui/model`` instead of a top-level ``config`` directory.
"""

